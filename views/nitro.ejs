<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Nitro Features - Discord Selfbot</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav>
    <div class="nav-brand">🎮 Discord Selfbot</div>
    <div class="nav-links">
      <a href="/">Dashboard</a>
      <a href="/user-info">Profile</a>
      <a href="/nitro" class="active">Nitro</a>
      <a href="/settings">Settings</a>
    </div>
  </nav>

  <div class="container">
    <div class="header-section">
      <h1>💎 Nitro Features Manager</h1>
      <p>Manage your Nitro subscription and server boosts</p>
    </div>

    <div class="glass-card" style="background: linear-gradient(135deg, #ff6b6b 0%, #feca57 100%);">
      <h3>💎 Nitro Status</h3>
      <div class="nitro-status">
        <p><strong>Type:</strong> <span class="nitro-badge"><%= nitroInfo.type %></span></p>
        <% if (nitroInfo.premiumSince) { %>
          <p><strong>Member Since:</strong> <%= new Date(nitroInfo.premiumSince).toLocaleDateString() %></p>
        <% } %>
      </div>
    </div>

    <div class="boosts-section">
      <h2>🚀 Server Boosts (<%= nitroInfo.boosts.length %>)</h2>
      <% if (nitroInfo.boosts.length === 0) { %>
        <div class="alert alert-info">
          <p>You haven't boosted any servers yet. Boost a server to support it and unlock perks!</p>
        </div>
      <% } else { %>
        <div class="boosts-grid">
          <% nitroInfo.boosts.forEach(boost => { %>
            <div class="boost-card">
              <h3>🚀 <%= boost.guildName %></h3>
              <p>Boosting since: <%= new Date(boost.since).toLocaleDateString() %></p>
            </div>
          <% }) %>
        </div>
      <% } %>
    </div>

    <div class="boost-form">
      <h2>🎁 Boost a Server</h2>
      <form id="boostServerForm">
        <div class="form-group">
          <label for="guildId">Server ID:</label>
          <input type="text" id="guildId" name="guildId" required placeholder="Enter server ID to boost">
        </div>
        <button type="submit" class="btn btn-primary">Boost Server</button>
      </form>
    </div>

    <div class="nitro-perks">
      <h2>✨ Nitro Perks</h2>
      <div class="perks-grid">
        <div class="perk-card">
          <h3>🎨 Custom Emoji</h3>
          <p>Use animated emoji anywhere</p>
        </div>
        <div class="perk-card">
          <h3>📤 File Uploads</h3>
          <p>Upload files up to 100MB</p>
        </div>
        <div class="perk-card">
          <h3>🎥 HD Streaming</h3>
          <p>Stream in 1080p @ 60fps</p>
        </div>
        <div class="perk-card">
          <h3>🎭 Profile Badge</h3>
          <p>Show off your Nitro badge</p>
        </div>
        <div class="perk-card">
          <h3>🖼️ Server Banner</h3>
          <p>Customize your profile</p>
        </div>
        <div class="perk-card">
          <h3>🚀 2 Server Boosts</h3>
          <p>Boost your favorite servers</p>
        </div>
      </div>
    </div>

    <div class="alert alert-info" style="margin-top: 2rem;">
      <p><strong>ℹ️ Note:</strong> Nitro features require an active Nitro subscription. Some features may be limited in selfbot implementations.</p>
    </div>
  </div>

  <script>
    document.getElementById('boostServerForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const formData = new FormData(e.target);
      const data = {
        guildId: formData.get('guildId')
      };
      
      try {
        const response = await fetch('/nitro/boost', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data)
        });
        
        const result = await response.json();
        if (result.success) {
          alert(result.message || 'Server boosted!');
          location.reload();
        } else {
          alert('Error: ' + result.error);
        }
      } catch (error) {
        alert('Error boosting server: ' + error.message);
      }
    });
  </script>

  <style>
    .nitro-status {
      font-size: 1.1rem;
    }

    .nitro-badge {
      background: linear-gradient(45deg, #ff6b6b, #feca57);
      padding: 0.25rem 0.75rem;
      border-radius: 12px;
      font-weight: bold;
      color: white;
    }

    .boosts-section {
      margin-top: 2rem;
    }

    .boosts-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 1.5rem;
      margin-top: 1rem;
    }

    .boost-card {
      background: var(--card-bg);
      border-radius: 12px;
      padding: 1.5rem;
      border-left: 4px solid #feca57;
    }

    .boost-card h3 {
      margin: 0 0 0.5rem 0;
    }

    .boost-form {
      background: var(--card-bg);
      border-radius: 12px;
      padding: 1.5rem;
      margin-top: 2rem;
    }

    .nitro-perks {
      margin-top: 2rem;
    }

    .perks-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: 1rem;
      margin-top: 1rem;
    }

    .perk-card {
      background: var(--card-bg);
      border-radius: 8px;
      padding: 1rem;
      text-align: center;
      transition: transform 0.2s;
    }

    .perk-card:hover {
      transform: translateY(-5px);
    }

    .perk-card h3 {
      margin: 0 0 0.5rem 0;
      font-size: 1.1rem;
    }

    .perk-card p {
      margin: 0;
      color: var(--text-secondary);
      font-size: 0.9rem;
    }
  </style>
</body>
</html>
