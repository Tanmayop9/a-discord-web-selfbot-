<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Custom Commands - Discord Web Selfbot</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/socket.io/socket.io.js"></script>
</head>
<body>
  <nav>
    <div class="nav-content">
      <a href="/" class="nav-brand">🎮 Discord Selfbot</a>
      <ul class="nav-links">
        <li><a href="/">Dashboard</a></li>
        <li><a href="/custom-commands">⚡ Commands</a></li>
        <li><a href="/auto-responder">🤖 Auto-Responder</a></li>
      </ul>
      <div class="user-info">
        <img src="<%= user.displayAvatarURL({ dynamic: true }) %>" alt="<%= user.tag %>" class="user-avatar">
        <span><%= user.tag %></span>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="card">
      <div class="card-header">⚡ Create Custom Command</div>
      
      <form onsubmit="event.preventDefault(); addCustomCommand();">
        <div class="grid grid-2">
          <div class="form-group">
            <label class="form-label">Trigger (command)</label>
            <input type="text" class="form-control" id="trigger" placeholder=".hello" required>
          </div>
          
          <div class="form-group">
            <label class="form-label">Response</label>
            <input type="text" class="form-control" id="response" placeholder="Hello World!" required>
          </div>
        </div>
        
        <button type="submit" class="btn btn-primary">Add Command</button>
      </form>
    </div>

    <div class="card">
      <div class="card-header">📋 Your Custom Commands (<%= customCommands.length %>)</div>
      
      <table class="table">
        <thead>
          <tr>
            <th>Trigger</th>
            <th>Response</th>
            <th>Created</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          <% customCommands.forEach(cmd => { %>
          <tr>
            <td><code><%= cmd.trigger %></code></td>
            <td><%= cmd.response %></td>
            <td><%= new Date(cmd.created).toLocaleDateString() %></td>
            <td>
              <button class="btn btn-sm btn-danger" onclick="deleteCustomCommand(<%= cmd.id %>)">Delete</button>
            </td>
          </tr>
          <% }); %>
        </tbody>
      </table>
      
      <% if (customCommands.length === 0) { %>
      <p class="text-center" style="color: var(--text-muted); padding: 40px;">
        No custom commands configured yet.
      </p>
      <% } %>
    </div>

    <div class="alert alert-info">
      ℹ️ <strong>How it works:</strong> When you send a message starting with the trigger, 
      it will be deleted and replaced with the response. Perfect for quick shortcuts!
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script>
    async function addCustomCommand() {
      const trigger = document.getElementById('trigger').value;
      const response = document.getElementById('response').value;
      
      try {
        await apiCall('/custom-commands/add', 'POST', { trigger, response });
        showNotification('Command added!', 'success');
        setTimeout(() => location.reload(), 1000);
      } catch (error) {
        // Error already shown
      }
    }
    
    async function deleteCustomCommand(id) {
      if (!confirm('Delete this command?')) return;
      
      try {
        await apiCall('/custom-commands/delete', 'POST', { id });
        showNotification('Command deleted!', 'success');
        setTimeout(() => location.reload(), 1000);
      } catch (error) {
        // Error already shown
      }
    }
  </script>
</body>
</html>
