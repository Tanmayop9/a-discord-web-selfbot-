<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Activity Logs - Discord Web Selfbot</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/socket.io/socket.io.js"></script>
</head>
<body>
  <nav>
    <div class="nav-content">
      <a href="/" class="nav-brand">🎮 Discord Selfbot</a>
      <ul class="nav-links">
        <li><a href="/">Dashboard</a></li>
        <li><a href="/activity-logs">📜 Activity Logs</a></li>
        <li><a href="/analytics">📊 Analytics</a></li>
      </ul>
      <div class="user-info">
        <img src="<%= user.displayAvatarURL({ dynamic: true }) %>" alt="<%= user.tag %>" class="user-avatar">
        <span><%= user.tag %></span>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="card">
      <div class="card-header flex-between">
        <span>📜 Activity Logs (Last 100)</span>
        <button class="btn btn-danger btn-sm" onclick="clearLogs()">Clear Logs</button>
      </div>
      
      <div class="message-list" style="max-height: 600px;">
        <% logs.forEach(log => { %>
        <div class="card" style="margin-bottom: 10px; padding: 10px;">
          <div class="flex-between">
            <div>
              <span class="badge badge-primary"><%= log.type %></span>
              <span style="color: var(--text-muted); margin-left: 10px; font-size: 0.9rem;">
                <%= new Date(log.timestamp).toLocaleString() %>
              </span>
            </div>
          </div>
          <% if (log.content) { %>
          <p style="margin-top: 10px; color: var(--text-primary); font-family: monospace; font-size: 0.9rem;">
            <%= log.content %>
          </p>
          <% } %>
          <div style="margin-top: 5px; font-size: 0.8rem; color: var(--text-muted);">
            Channel: <%= log.channelId %> | Author: <%= log.authorId %>
          </div>
        </div>
        <% }); %>
      </div>
      
      <% if (logs.length === 0) { %>
      <p class="text-center" style="color: var(--text-muted); padding: 40px;">
        No activity logged yet.
      </p>
      <% } %>
    </div>

    <div class="alert alert-info">
      ℹ️ <strong>Activity Logs</strong> capture all your Discord interactions including messages, commands, and more. 
      The last 1000 activities are stored in memory.
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script>
    async function clearLogs() {
      if (!confirm('Clear all activity logs?')) return;
      
      try {
        await apiCall('/activity-logs/clear', 'POST');
        showNotification('Logs cleared!', 'success');
        setTimeout(() => location.reload(), 1000);
      } catch (error) {
        // Error already shown
      }
    }
  </script>
</body>
</html>
