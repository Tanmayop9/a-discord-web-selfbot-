<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Server Statistics - Discord Web Selfbot</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/socket.io/socket.io.js"></script>
</head>
<body>
  <nav>
    <div class="nav-content">
      <a href="/" class="nav-brand">🎮 Discord Selfbot</a>
      <ul class="nav-links">
        <li><a href="/">Dashboard</a></li>
        <li><a href="/servers">🏰 Servers</a></li>
        <li><a href="/server-stats/<%= guild.id %>">📊 Stats</a></li>
      </ul>
      <div class="user-info">
        <img src="<%= user.displayAvatarURL({ dynamic: true }) %>" alt="<%= user.tag %>" class="user-avatar">
        <span><%= user.tag %></span>
      </div>
    </div>
  </nav>

  <div class="container fade-in">
    <div class="card-enhanced" style="background: linear-gradient(135deg, var(--bg-secondary), var(--bg-tertiary));">
      <h1 style="font-size: 2rem; margin-bottom: 10px;"><%= stats.name %></h1>
      <p style="color: var(--text-muted);">Server Statistics & Information</p>
    </div>

    <div class="grid grid-4">
      <div class="stat-card">
        <div style="position: relative; z-index: 1;">
          <p class="stat-label">Total Members</p>
          <p class="stat-value"><%= stats.memberCount %></p>
        </div>
      </div>
      
      <div class="stat-card">
        <div style="position: relative; z-index: 1;">
          <p class="stat-label">Channels</p>
          <p class="stat-value"><%= stats.channelCount %></p>
        </div>
      </div>
      
      <div class="stat-card">
        <div style="position: relative; z-index: 1;">
          <p class="stat-label">Roles</p>
          <p class="stat-value"><%= stats.roleCount %></p>
        </div>
      </div>
      
      <div class="stat-card">
        <div style="position: relative; z-index: 1;">
          <p class="stat-label">Emojis</p>
          <p class="stat-value"><%= stats.emojiCount %></p>
        </div>
      </div>
    </div>

    <div class="grid grid-2">
      <div class="card-enhanced">
        <div class="card-header">💎 Boost Information</div>
        
        <table class="table">
          <tr>
            <td><strong>Boost Level:</strong></td>
            <td>Level <%= stats.boostLevel %></td>
          </tr>
          <tr>
            <td><strong>Boost Count:</strong></td>
            <td><%= stats.boostCount %> boosts</td>
          </tr>
        </table>
        
        <div class="progress-bar" style="margin-top: 15px;">
          <div class="progress-fill" style="width: <%= (stats.boostLevel / 3) * 100 %>%"></div>
        </div>
        <p style="text-align: center; color: var(--text-muted); font-size: 0.85rem; margin-top: 8px;">
          Boost Progress: Level <%= stats.boostLevel %> / 3
        </p>
      </div>
      
      <div class="card-enhanced">
        <div class="card-header">ℹ️ Server Information</div>
        
        <table class="table">
          <tr>
            <td><strong>Created:</strong></td>
            <td><%= stats.createdAt %></td>
          </tr>
          <tr>
            <td><strong>Owner ID:</strong></td>
            <td><code><%= stats.owner %></code></td>
          </tr>
        </table>
      </div>
    </div>

    <div class="card-enhanced">
      <div class="card-header">📈 Growth Metrics</div>
      
      <p style="color: var(--text-secondary); text-align: center; padding: 40px;">
        Member growth tracking coming soon!
      </p>
    </div>

    <div style="text-align: center; margin-top: 20px;">
      <a href="/server/<%= guild.id %>" class="btn btn-secondary">← Back to Server</a>
      <a href="/backup/<%= guild.id %>" class="btn btn-primary">💾 Backup Server Structure</a>
    </div>
  </div>

  <script src="/js/app.js"></script>
</body>
</html>
