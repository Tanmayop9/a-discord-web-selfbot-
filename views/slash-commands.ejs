<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Slash Commands - Discord Web Selfbot</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/socket.io/socket.io.js"></script>
</head>
<body>
  <nav>
    <div class="nav-content">
      <a href="/" class="nav-brand">🎮 Discord Selfbot</a>
      <ul class="nav-links">
        <li><a href="/">Dashboard</a></li>
        <li><a href="/slash-commands">⚔️ Slash Commands</a></li>
      </ul>
      <div class="user-info">
        <img src="<%= user.displayAvatarURL({ dynamic: true }) %>" alt="<%= user.tag %>" class="user-avatar">
        <span><%= user.tag %></span>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="card">
      <div class="card-header">⚔️ Available Slash Commands (<%= commands.length %>)</div>
      
      <table class="table">
        <thead>
          <tr>
            <th>Command</th>
            <th>Description</th>
            <th>Server</th>
            <th>Command ID</th>
          </tr>
        </thead>
        <tbody>
          <% commands.forEach(cmd => { %>
          <tr>
            <td><code>/<%= cmd.name %></code></td>
            <td><%= cmd.description %></td>
            <td><%= cmd.guildName %></td>
            <td><code><%= cmd.id %></code></td>
          </tr>
          <% }); %>
        </tbody>
      </table>
      
      <% if (commands.length === 0) { %>
      <p class="text-center" style="color: var(--text-muted); padding: 40px;">
        No slash commands found in your servers.
      </p>
      <% } %>
    </div>

    <div class="alert alert-info">
      ℹ️ <strong>Note:</strong> This displays all slash commands available in servers you're in. 
      You can use these commands in Discord as normal.
    </div>
  </div>

  <script src="/js/app.js"></script>
</body>
</html>
