<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Settings - Discord Web Selfbot</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/socket.io/socket.io.js"></script>
</head>
<body>
  <nav>
    <div class="nav-content">
      <a href="/" class="nav-brand">🎮 Discord Selfbot</a>
      <ul class="nav-links">
        <li><a href="/">Dashboard</a></li>
        <li><a href="/servers">Servers</a></li>
        <li><a href="/settings">Settings</a></li>
      </ul>
      <div class="user-info">
        <img src="<%= user.displayAvatarURL({ dynamic: true }) %>" alt="<%= user.tag %>" class="user-avatar">
        <span><%= user.tag %></span>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="card">
      <div class="card-header">⚙️ User Settings</div>
      
      <div class="form-group">
        <label class="form-label">Theme</label>
        <select class="form-control" id="theme" onchange="updateSetting('theme', this.value)">
          <option value="dark" <%= settings.theme === 'dark' ? 'selected' : '' %>>Dark</option>
          <option value="light" <%= settings.theme === 'light' ? 'selected' : '' %>>Light</option>
        </select>
      </div>
      
      <div class="form-group">
        <label class="form-label">Language</label>
        <select class="form-control" id="locale" onchange="updateSetting('locale', this.value)">
          <option value="en-US" <%= settings.locale === 'en-US' ? 'selected' : '' %>>English (US)</option>
          <option value="en-GB" <%= settings.locale === 'en-GB' ? 'selected' : '' %>>English (UK)</option>
          <option value="es-ES" <%= settings.locale === 'es-ES' ? 'selected' : '' %>>Spanish</option>
          <option value="fr-FR" <%= settings.locale === 'fr-FR' ? 'selected' : '' %>>French</option>
          <option value="de-DE" <%= settings.locale === 'de-DE' ? 'selected' : '' %>>German</option>
          <option value="ja-JP" <%= settings.locale === 'ja-JP' ? 'selected' : '' %>>Japanese</option>
        </select>
      </div>
      
      <div class="form-group">
        <label class="form-label">
          <input type="checkbox" id="messageDisplayCompact" <%= settings.messageDisplay ? 'checked' : '' %> onchange="updateSetting('messageDisplayCompact', this.checked)">
          Compact Message Display
        </label>
      </div>
      
      <div class="form-group">
        <label class="form-label">
          <input type="checkbox" id="inlineAttachmentMedia" <%= settings.inlineAttachmentMedia ? 'checked' : '' %> onchange="updateSetting('inlineAttachmentMedia', this.checked)">
          Show Inline Attachments
        </label>
      </div>
      
      <div class="form-group">
        <label class="form-label">
          <input type="checkbox" id="inlineEmbedMedia" <%= settings.inlineEmbedMedia ? 'checked' : '' %> onchange="updateSetting('inlineEmbedMedia', this.checked)">
          Show Inline Embeds
        </label>
      </div>
      
      <div class="form-group">
        <label class="form-label">
          <input type="checkbox" id="gifAutoPlay" <%= settings.gifAutoPlay ? 'checked' : '' %> onchange="updateSetting('gifAutoPlay', this.checked)">
          Auto-play GIFs
        </label>
      </div>
      
      <div class="form-group">
        <label class="form-label">
          <input type="checkbox" id="renderEmbeds" <%= settings.renderEmbeds ? 'checked' : '' %> onchange="updateSetting('renderEmbeds', this.checked)">
          Render Embeds
        </label>
      </div>
      
      <div class="form-group">
        <label class="form-label">
          <input type="checkbox" id="renderReactions" <%= settings.renderReactions ? 'checked' : '' %> onchange="updateSetting('renderReactions', this.checked)">
          Show Reactions
        </label>
      </div>
      
      <div class="form-group">
        <label class="form-label">
          <input type="checkbox" id="animateEmoji" <%= settings.animateEmoji ? 'checked' : '' %> onchange="updateSetting('animateEmoji', this.checked)">
          Animate Emoji
        </label>
      </div>
      
      <div class="form-group">
        <label class="form-label">
          <input type="checkbox" id="showCurrentGame" <%= settings.showCurrentGame ? 'checked' : '' %> onchange="updateSetting('showCurrentGame', this.checked)">
          Display Current Activity
        </label>
      </div>
      
      <div class="form-group">
        <label class="form-label">
          <input type="checkbox" id="enableTTSCommand" <%= settings.enableTTSCommand ? 'checked' : '' %> onchange="updateSetting('enableTTSCommand', this.checked)">
          Enable TTS Command
        </label>
      </div>
    </div>

    <div class="alert alert-info">
      ℹ️ <strong>Note:</strong> Some settings may require Nitro or specific permissions to take effect.
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script>
    async function updateSetting(setting, value) {
      try {
        await apiCall('/update-settings', 'POST', { setting, value });
        showNotification('Setting updated!', 'success');
      } catch (error) {
        // Error already shown
      }
    }
  </script>
</body>
</html>
