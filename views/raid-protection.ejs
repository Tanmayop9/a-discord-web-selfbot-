<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Raid Protection - Discord Web Selfbot</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/socket.io/socket.io.js"></script>
</head>
<body>
  <nav>
    <div class="nav-content">
      <a href="/" class="nav-brand">🎮 Discord Selfbot</a>
      <ul class="nav-links">
        <li><a href="/">Dashboard</a></li>
        <li><a href="/raid-protection">🛡️ Raid Protection</a></li>
        <li><a href="/server-stats">📊 Server Stats</a></li>
      </ul>
      <div class="user-info">
        <img src="<%= user.displayAvatarURL({ dynamic: true }) %>" alt="<%= user.tag %>" class="user-avatar">
        <span><%= user.tag %></span>
      </div>
    </div>
  </nav>

  <div class="container fade-in">
    <% if (raidProtection.enabled) { %>
    <div class="alert alert-success">
      ✅ Raid Protection is <strong>ENABLED</strong>
    </div>
    <% } else { %>
    <div class="alert alert-warning">
      ⚠️ Raid Protection is <strong>DISABLED</strong>
    </div>
    <% } %>

    <div class="card-enhanced">
      <div class="card-header">🛡️ Raid Protection Settings</div>
      
      <form onsubmit="event.preventDefault(); updateRaidProtection();">
        <div class="form-group">
          <label class="form-label">Status</label>
          <div class="toggle-switch">
            <input type="checkbox" id="enabled" <%= raidProtection.enabled ? 'checked' : '' %>>
            <span class="toggle-slider"></span>
          </div>
        </div>
        
        <div class="grid grid-2">
          <div class="form-group">
            <label class="form-label">Join Threshold</label>
            <input type="number" class="form-control" id="joinThreshold" value="<%= raidProtection.joinThreshold %>" min="1" max="100">
            <small style="color: var(--text-muted);">Maximum joins within time window</small>
          </div>
          
          <div class="form-group">
            <label class="form-label">Time Window (seconds)</label>
            <input type="number" class="form-control" id="timeWindow" value="<%= raidProtection.timeWindow %>" min="10" max="300">
            <small style="color: var(--text-muted);">Time period to check joins</small>
          </div>
        </div>
        
        <div class="form-group">
          <label class="form-label">Action on Detection</label>
          <select class="form-control" id="action">
            <option value="kick" <%= raidProtection.action === 'kick' ? 'selected' : '' %>>Kick</option>
            <option value="ban" <%= raidProtection.action === 'ban' ? 'selected' : '' %>>Ban</option>
            <option value="notify" <%= raidProtection.action === 'notify' ? 'selected' : '' %>>Notify Only</option>
          </select>
        </div>
        
        <button type="submit" class="btn btn-primary">💾 Save Settings</button>
      </form>
    </div>

    <div class="card-enhanced">
      <div class="card-header">ℹ️ How It Works</div>
      
      <ul style="color: var(--text-secondary); line-height: 2;">
        <li>Monitors server joins in real-time</li>
        <li>Triggers when join threshold is exceeded within time window</li>
        <li>Takes specified action (kick, ban, or notify)</li>
        <li>Helps protect your servers from raid attacks</li>
      </ul>
    </div>

    <div class="alert alert-danger">
      ⚠️ <strong>Note:</strong> You must have appropriate permissions in the server to kick/ban users.
      This feature requires the selfbot to be in the target servers.
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script>
    async function updateRaidProtection() {
      const enabled = document.getElementById('enabled').checked;
      const joinThreshold = document.getElementById('joinThreshold').value;
      const timeWindow = document.getElementById('timeWindow').value;
      const action = document.getElementById('action').value;
      
      try {
        await apiCall('/raid-protection/toggle', 'POST', { 
          enabled, 
          joinThreshold, 
          timeWindow, 
          action 
        });
        showNotification('Raid protection settings updated!', 'success');
        setTimeout(() => location.reload(), 1000);
      } catch (error) {
        // Error already shown
      }
    }
  </script>
</body>
</html>
