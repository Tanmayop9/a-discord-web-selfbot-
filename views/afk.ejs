<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AFK Mode - Discord Web Selfbot</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/socket.io/socket.io.js"></script>
</head>
<body>
  <nav>
    <div class="nav-content">
      <a href="/" class="nav-brand">🎮 Discord Selfbot</a>
      <ul class="nav-links">
        <li><a href="/">Dashboard</a></li>
        <li><a href="/afk">🌙 AFK Mode</a></li>
        <li><a href="/auto-responder">🤖 Auto-Responder</a></li>
      </ul>
      <div class="user-info">
        <img src="<%= user.displayAvatarURL({ dynamic: true }) %>" alt="<%= user.tag %>" class="user-avatar">
        <span><%= user.tag %></span>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="card">
      <div class="card-header">🌙 AFK Mode</div>
      
      <% if (afkMode.enabled) { %>
      <div class="alert alert-success">
        ✅ AFK Mode is currently <strong>ENABLED</strong><br>
        Message: <%= afkMode.message %><br>
        Since: <%= new Date(afkMode.since).toLocaleString() %> 
        (<%= Math.floor((Date.now() - afkMode.since) / 1000 / 60) %> minutes ago)
      </div>
      <% } else { %>
      <div class="alert alert-warning">
        AFK Mode is currently <strong>DISABLED</strong>
      </div>
      <% } %>
      
      <form onsubmit="event.preventDefault(); toggleAFK();">
        <div class="form-group">
          <label class="form-label">AFK Message</label>
          <input type="text" class="form-control" id="afkMessage" 
                 value="<%= afkMode.message %>" 
                 placeholder="I am currently AFK">
        </div>
        
        <button type="submit" class="btn btn-<%= afkMode.enabled ? 'danger' : 'primary' %>">
          <%= afkMode.enabled ? 'Disable' : 'Enable' %> AFK Mode
        </button>
      </form>
    </div>

    <div class="card">
      <div class="card-header">ℹ️ How AFK Mode Works</div>
      
      <ul style="color: var(--text-secondary); line-height: 2;">
        <li>When enabled, you'll automatically respond to mentions</li>
        <li>Response includes your custom AFK message and how long you've been AFK</li>
        <li>Perfect for when you're away but want people to know</li>
        <li>Disable manually when you return</li>
      </ul>
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script>
    async function toggleAFK() {
      const message = document.getElementById('afkMessage').value;
      
      try {
        const result = await apiCall('/afk/toggle', 'POST', { message });
        showNotification(result.enabled ? 'AFK Mode enabled!' : 'AFK Mode disabled!', 'success');
        setTimeout(() => location.reload(), 1000);
      } catch (error) {
        // Error already shown
      }
    }
  </script>
</body>
</html>
